<%= form_with(model: anottation) do |form| %>
  <% if anottation.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(anottation.errors.count, "error") %> prohibited this anottation from being saved:</h2>

      <ul>
        <% anottation.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-floating m-2">
    <%= form.text_area :body, class: "form-control"%>
    <%= form.label :body, style: "display: block" %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
